import 'package:flutter/material.dart';

class Apoitment extends StatelessWidget {
  const Apoitment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Đặt hẹn"
      )
    );
  }
}
